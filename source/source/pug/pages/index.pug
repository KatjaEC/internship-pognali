extends ../components/base/layout

block variables
  - const title = "Погнали - сервис поиска попутчиков для путешествий" //- на страницах кроме главной используйте О нас — ProjectName
  - const wrapperMods = ""
  - const hasHeader = true
  - const headerMods = "header--index"
  - const hasFooter = true
  - const footerMods = ""
  - const currentUrl = "index.html" //- для остальных страниц пишем пустую строку или индекс активного headerLinks

block main
  main
    h1.visually-hidden Главная страница сайта "Погнали".
    //- на странице запрещен "голый" код
    //- все блоки вставляются из папки pug/components/blocks
    +intro()
    +about-us()
    +destinations()
    +companions()
    +add-profile()
    +registration()
    +map()

block modals
  +modal()
